> 📌 **本项目已停止维护（No longer maintained）。** 当前仓库代码即最终状态，作者不会再做更新，也不会回复 issue / PR。若你需要其他版本或想继续迭代，请自行 fork 修改——MIT 许可允许自由使用、修改与再分发。

# FA-ASMR

日语 ASMR 配音**强制对齐（打轴）**工具。

> ⚠️ **本项目是 [moriwx/FA-Kara](https://github.com/moriwx/FA-Kara) 的重度魔改分叉（fork）。**
>
> 原项目是一个基于注音歌词 + 人声音频的自动打轴工具，主要处理日语**歌曲**。本工具在其代码结构基础上**派生、重构并重新定位**：底层沿用同一套 MMS_FA 强制对齐核心，但功能由「歌曲打轴」改为「ASMR 配音打轴」，并新增了 GUI、LoRA 适配器、自动注音、CPU 量化等大量模块。
>
> 原项目使用 **MIT License（Copyright (c) 2025 moriwx）**，本工具沿用其代码骨架并保留该署名与许可。

---

## 这个项目是做什么的

原 FA-Kara 的用途是「歌曲歌词 → 时间轴字幕」。在本项目中，它被**重新定位**为：

> 输入 **日语 ASMR 台本（含拟声 / 喘息 / 呻吟等口播文本）** 与 **对应的人声配音音频**，
> 自动计算每一句台本在音频里出现的**精确起止时间**，产出标准对齐结果（LRC / 时间轴）。

核心流程：

```
原始台本(.txt 含 {漢字|かな} 注音)
   │  RubyAnnotator 自动注音 (UniDic + yomikata 异读消歧)
   ▼
   │  haruraw2norm 罗马音 / 假名标准化 → MMS_FA token
   ▼
人声音频(.wav/.mp3)
   │  MMS_FA + LoRA 强制对齐器 (强制单调覆盖 CTC)
   ▼
每个 token / 每行的精确时间戳
   │  后处理 (auto_repeat 拟声行修复 / 时长约束 / 信道择优)
   ▼
标准 LRC / 时间轴
```

---

## ⚠️ 使用前提与限制（务必先读）

### 1. 台本必须**提前手工修改**
对齐器只会对「写出来、且音频里真正发出来的内容」负责。台本中**不含实际发音**的部分必须提前删掉或清理，例如：
- 说话人前缀（`Ａ：`、`Ｂ：` 等）
- 舞台提示、动作描写（`（小声）`、`■场记`、括号里的非口播内容）
- 纯数字场号、章节标记
- 与音频对不上的内容

如果台本里写了但音频里没念（或反之），对齐会把多余 token 硬塞进相邻句子，造成该行及后续行的整体错位。

### 2. **不支持乱序台本**
对齐严格按「台本行顺序 = 音频时间顺序」做单调覆盖。如果台本顺序与音频实际播放顺序不一致（倒叙、插叙、跳接），**结果会整体错乱**。本工具不处理乱序。

### 3. 效果有限 / 并不完善
- 本项目**仍在开发中，并不完善**：已知有多项实验路径（如逐行窗口重对齐、递推窗口等）经实测无效已被移除或默认关闭；部分边界场景（高密度拟声行、极长沉默）仍有较高误差。
- 当前对齐**起点可信、终点（时长）偏紧**：CTC 会把孤立/弱发射 token 压短，单 token 时长可能偏塌缩，长喘息/拟声行尤其明显。
- 详见下方「[实测数据](#实测数据)」——中位误差虽低，但仍有约一成行误差 > 1s，且**尾部大幅错误行（十秒级）目前无根治方案**，属 CTC 帧分配的结构性局限。

---

## 与原项目 FA-Kara 的主要差异

| 维度 | 原 FA-Kara | 本仓库 FA-ASMR |
|------|-----------|----------------|
| 目标内容 | 日语**歌曲**歌词 | ASMR **口播/拟声/喘息**台词 |
| 注音引擎 | pykakasi / Janome | UniDic(sudachipy) + yomikata 异读层 + 部首字符修复 |
| 对齐模型 | MMS_FA 基座（可选 yohane 微调） | MMS_FA + **自研最小 LoRA**（不依赖 peft 库） |
| 重复行处理 | 无 | `auto_repeat` 自动检测拟声循环行并展开 |
| 设备/打包 | 纯脚本 | GPU/CPU 自适应 + INT8 量化 + exe 打包 + torch 引导安装 |
| 交互形态 | 命令行 `main.py` | `FA-ASMR_GUI.py`（拖放批量）+ CLI |

> 已证无效的实验路径（如 `anchor_realign`、`region_align`、`two_pass` 递推窗口重对齐等）已被移除或默认关闭，相关结论沉淀于 `_hr_optimize/eval_out/`（该目录为内部评测产物，不随工具发布）。

---

## 你要发布的内容

**本工具要发布 / 分发的，就是 `FA-ASMR-Release/` 这一个文件夹及其内部全部文件（对齐工具）。**

```text
FA-ASMR-Release/                  # ★ 发布的就是这一个文件夹及其全部内部文件（对齐工具）
├── FA-ASMR_GUI.py              #   GUI 主程序（拖放批量对齐）
├── align.py                    #   强制对齐主入口
├── align_model.py              #   MMS_FA 模型缓存 / LoRA 适配器 / emission 计算
├── align_utils.py              #   token 工具 / span 打分 / OnoWave 波形计数 / 重复行检测
├── align_post.py               #   后处理 (auto_repeat 等)
├── haruraw2norm.py             #   注音文本 → 罗马音/假名 → MMS_FA token
├── furigana/
│   └── fa_asmr_converter.py    #   自动注音引擎 (UniDic + yomikata)
├── advanced_config.json        #   底层系统配置（CPU 线程/INT8/分块，手编不进 GUI）
├── fa_asmr_settings.json       #   GUI 用户设置
├── torch_bootstrap.py          #   缺失 torch 时自动引导安装 / 借系统 CUDA torch
├── _build_exe.py / _build_exe.bat  # PyInstaller 打包（onedir + embedded_torch）
└── models/                     #   MMS_FA 权重 model.pt（需手动放置，不进包）
```

---

## 快速开始

### 方式一：GUI（推荐）

1. 从 `FA-ASMR-Release/` 运行：
   ```shell
   cd FA-ASMR-Release
   python FA-ASMR_GUI.py
   # 或双击打包后的 FA-ASMR.exe
   ```
2. **先按上文「使用前提」手工整理台本**（去掉非发音内容、保证与音频同序）。
3. 拖入「台本 `.txt`（可含 `{漢字|かな}` 注音）+ 音频 `.wav/.mp3`」，按文件名自动配对。
4. 点击运行，输出标准 LRC / 时间轴。
   - 首次缺失 PyTorch 时，GUI 会弹出引导安装（可借系统 CUDA 加速，否则回退内置 CPU 版）。
   - `MMS_FA` 权重 `model.pt` 需放在 `FA-ASMR-Release/models/hub/checkpoints/`（exe 同级 `models/`），**不随 exe 打包**。

### 方式二：命令行

```shell
python FA-ASMR_GUI.py --cli --script 台本.txt --audio 音频.wav [--lora checkpoints/E30.pt]
```

---

## 进阶配置（底层系统选项）

GUI 高级面板之外的「系统层」旋钮集中在 **`FA-ASMR-Release/advanced_config.json`**：

| 键 | 含义 | 默认 |
|----|------|------|
| `cpu_threads` | CPU 前向线程数（null = 物理核数） | null |
| `quantize_int8` | CPU INT8 动态量化（null = 自动） | null |
| `cpu_chunk_cap` | CPU 单块帧数上限（控制内存/速度权衡） | 2000 |

该文件**独立于 GUI 设置、只被读取不会被 GUI 写回**，手编内容不会被保存覆盖；重新打包时模板仅在不冲突时复制。

---

## 实测数据

以下为内部评测快照（样本：RJ01553800 多轨人工标轴，共 6 轨约 630 行，**排除 07 轨剪辑合集**）。仅供参考，不代表对所有素材的保证效果。

**系统评测（baseline：VAD 关 + auto_repeat 收敛）**
| 指标 | 数值 |
|------|------|
| 中位绝对误差 | 107 ms |
| P90 误差 | 1521 ms |
| ±200ms 命中率 | 73.0% |
| ±500ms 命中率 | 81.3% |
| ±1s 命中率 | 85.1% |
| 跨行漂移 | +0.5 ms/行（可忽略） |
| 时长比（pred/gt） | 0.67（终点偏紧） |
| 重复型行 | 检测 18 个，修复 11 个塌缩行 |

**LoRA 权重对比（global 配置，630 行，排除 07 轨）**
| 权重 | 中位\|e\| | P90 | ±200ms | 崩塌率(>1s) |
|------|---------|-----|--------|------------|
| **kbest_E30_loss0.5050（最优）** | 110 ms | 1670 ms | 73.3% | 14.8% |
| kbest_E25_loss0.6473 | 110 ms | 1900 ms | 71.3% | 17.6% |
| 无 LoRA | 110 ms | 3420 ms | 68.1% | 21.9% |
| best_model(E50) | 120 ms | 2440 ms | 68.1% | 20.0% |

> 要点：
> - **精度已触底（中位 ≈ 110 ms）**，这是 CTC 尖峰 + 40ms 帧率 + 字幕人工留余量的下限，常规优化难以再降。
> - **LoRA 的作用是抑制尾部大幅错误（崩塌率），而非提升常规精度**。继续压训练 loss 反而过拟合劣化，E30 为过拟合拐点。
> - **仍有约 85 行误差 > 1s**，集中在台本拟声/喘息行（token 密度与真实声学不匹配），属 CTC 帧分配的结构性局限，目前无后处理根治方案。
> - 07 轨（総集編/剪辑合集，台词乱序重复）所有配置崩塌 62~79%，属数据性质问题——再次印证「**不支持乱序台本**」。

---

## 许可与署名

- 原项目 **FA-Kara** 由 [moriwx](https://github.com/moriwx/FA-Kara) 以 **MIT License** 发布（Copyright (c) 2025 moriwx），本工具沿用其代码骨架并保留该署名与许可。
- 本仓库在 FA-Kara 代码骨架之上进行了大量改写与功能扩展，同样以 MIT 精神对外提供，但**与原项目的后续更新无关联**。

---

> 🤖 **声明**：本仓库（FA-ASMR 对齐工具及其注音 / 对齐 / 打包相关代码）由 **AI（编程助手）在用户指导下编写与迭代**。代码、配置与实验脚本均为 AI 生成产物的累积结果，可能包含未经人工逐行复核的实现细节；项目目前**并不完善**，使用时请自行评估风险。
